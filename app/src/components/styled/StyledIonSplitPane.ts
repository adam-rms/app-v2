import { IonSplitPane } from "@ionic/react";
import styled from "styled-components";

const StyledIonSplitPane = styled(IonSplitPane)`
  --side-width: 300px;
  --side-max-width: 300px;
`;

export default StyledIonSplitPane;
